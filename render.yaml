# Build fix: Testing Render builds
# Render.com Infrastructure as Code
# Cost: $0/month (Free tier)
# Note: Free web services spin down after 15 min inactivity
# Note: Backend requires MongoDB - use MongoDB Atlas free tier (M0)

services:
  # Frontend - React/Vite Static Site (FREE)
  - type: web
    name: save-a-stray-client
    runtime: static
    buildCommand: cd client && npm ci && npm run build
    staticPublishPath: client/dist
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false

  # Backend - Express/GraphQL/TypeScript API (FREE)
  - type: web
    name: save-a-stray-api
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: MONGO_URI
        sync: false
      - key: SECRET_OR_KEY
        generateValue: true
      - key: GOOG_CLIENT
        sync: false
      - key: GOOG_SECRET
        sync: false
      - key: FBOOK_KEY
        sync: false
      - key: FBOOK_CLIENT
        sync: false
      - key: CORS_ORIGIN
        sync: false
